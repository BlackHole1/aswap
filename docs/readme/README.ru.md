<div align="center">

# aswap

[English](../../README.md) | [简体中文](README.zh-CN.md) | [繁體中文](README.zh-TW.md) | [日本語](README.ja.md) | [한국어](README.ko.md) | Русский | [Français](README.fr.md) | [Español](README.es.md)

Переключение аккаунтов для Claude Code: [claude-swap](https://github.com/realiti4/claude-swap) (`cswap`) с поддерживаемым набором исправлений поверх него.

</div>

`cswap` переключает между несколькими аккаунтами Claude, отслеживает окна использования каждого из них и может перевести вас на аккаунт с наибольшим запасом. aswap поставляет тот же инструмент, собранный из неизмененного исходного кода upstream плюс небольшая, удобная для ревью серия патчей для проблем, которые upstream еще не исправил. Вы получаете знакомую команду `cswap`, только без шероховатостей.

## Название

aswap - сокращение от **account swap** (переключение аккаунтов), то есть от того, что делает инструмент. Поставьте его рядом с `cswap`, и `a` окажется еще и единственным, что добавляет этот проект: aswap - это cswap плюс серия патчей, и ничего больше. А по мере того как Claude Code превращается в универсальную среду для агентов, в `a` остается место и для аккаунтов, которые приходят вместе с ней: аккаунтов AI и аккаунтов агентов.

## Установка

```bash
uv tool install aswap  # или: pipx install aswap
aswap --version
```

`aswap` принимает все, что принимает `cswap`. Он ставится рядом с upstream `cswap`, не трогая его. Чтобы имя `cswap` тоже запускало aswap:

```bash
aswap link  # создает лаунчер cswap рядом с aswap; aswap unlink удаляет его
```

`aswap link` не перезаписывает `cswap`, который создал не он: сначала удалите upstream (`uv tool uninstall claude-swap`) или передайте `--force`.

## Обновление

```bash
aswap upgrade  # uv tool upgrade aswap или pipx upgrade aswap, в зависимости от способа установки
```

Сборка из исходников и замена `cswap` из PyPI пропатченной сборкой на месте описаны в [CONTRIBUTING.md](../../CONTRIBUTING.md).

## Что исправляют патчи

| патч                                                                                                                                             | проблема                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [Следовать за настоящим владельцем учетных данных](../../patches/fix_follow_the_live_credentials_owner_when_the_config_names_another_slot.patch) | Устаревший `~/.claude.json` может указывать на аккаунт B, пока в keychain лежит токен аккаунта A. Upstream ошибочно помечает A как "re-login needed" и перестает обновлять B. Патч следует за настоящим владельцем и запоминает ответ между запусками.                                                                                                                                                                                                                                             |
| [Следовать за установленным дистрибутивом](../../patches/feat_report_check_and_upgrade_the_distribution_this_install_came_from.patch)            | Версия, проверка обновлений и `upgrade` жестко прописывают `claude-swap`; установленные как `aswap`, они ломаются. Патч следует за тем дистрибутивом, который действительно предоставляет код.                                                                                                                                                                                                                                                                                                     |
| [Обновить usage по запросу](../../patches/feat_add_a_refresh_command_that_fetches_usage_now_one_account_at_a_time.patch)                         | Upstream запрашивает usage только по своему расписанию: пока serve TTL, план опроса и backoff после ошибки не разрешат новый запрос, все экраны показывают кэш, а срок повтора от сервера не показывается вовсе. `cswap refresh [аккаунт ...]` запрашивает сейчас, каждый аккаунт независимо, и сообщает, когда ограниченный аккаунт может повторить.                                                                                                                                              |
| [Слияние истории без потерь](../../patches/fix_set_aside_a_differing_history_file_instead_of_dropping_the_profiles_copy.patch)                   | Когда `cswap run` сливает собственную историю профиля в `~/.claude`, upstream считает любой одноимённый файл дубликатом и удаляет копию профиля, не сравнивая содержимое. Сессия, продолженная после отключения общей истории, или `memory/MEMORY.md` проекта теряются молча. Патч отбрасывает копию только при побайтовом совпадении, а отличающуюся откладывает в `history-conflicts/`.                                                                                                          |
| [Одна история диалогов на все аккаунты](../../patches/feat_share_one_conversation_history_across_accounts_by_default.patch)                      | У каждого аккаунта свой `projects/`, если не передавать `--share-history` при каждом запуске, а когда аккаунт и есть текущий логин по умолчанию, `cswap run` вовсе обходит профиль. Сессии одного аккаунта оказываются в двух местах, и после смены аккаунта по умолчанию `claude --resume` не находит диалог (upstream #239). Патч делает общую историю умолчанием, хранит выбор в настройке `session.shareHistory` и подмешивает историю профиля в `~/.claude` даже на пути логина по умолчанию. |

Каждый патч - один коммит, сообщение которого объясняет проблему upstream. Полный список в [patches/.patches](../../patches/.patches).

## Как это собрано

Upstream не форкается. Он закреплен на одном коммите в `upstream.json` и клонируется без изменений в `cswap/`, а каждое изменение хранится в `patches/` как обычный патч для `git am` и применяется в порядке, указанном в `patches/.patches`. Так же [Electron управляет своими патчами для Node и Chromium](https://github.com/electron/electron/tree/main/patches): дельта остается небольшой и читаемой, а переход на новый upstream - это rebase серии, а не merge. Подробности, команды и рабочий процесс с патчами описаны в [CONTRIBUTING.md](../../CONTRIBUTING.md).

## Лицензия

MIT, как и у claude-swap. См. [LICENSE](../../LICENSE).
